<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-900 font-sans flex items-center justify-center min-h-screen">
    <div class="w-full max-w-md bg-white p-8 rounded-lg shadow-lg">
        <h5 class="mb-5 text-xl font-semibold text-center">Update Your Post</h5>
        <form method="post" action="/update/<%=post._id%>" class="space-y-4">
            <textarea class="w-full h-32 rounded-lg border border-gray-300 p-4 bg-white focus:border-blue-500 outline-none resize-none shadow-sm" placeholder="What's on your mind?" name="content" required><%=post.content%></textarea>
            <input class="w-full px-4 py-2 bg-yellow-500 hover:bg-yellow-600 text-black rounded-lg cursor-pointer transition shadow-md" type="submit" value="Update Post"> 
        </form>
        <div class="mt-6 flex justify-center">
            <a class="bg-red-600 hover:bg-red-700 transition px-4 py-2 rounded-lg text-sm shadow-md text-white" href="/logout">Logout</a>
        </div>
    </div>
</body>
</html>
